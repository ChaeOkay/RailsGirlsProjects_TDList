<tbody>
  <% @lists.each do |list| %>
    <h3>
      <span style="color:#999999"><%= list.id %></span>  
      <%= link_to list.name, task_path(list.id) %> 
      <small style="color:#999999"><%=list.list_status%></small>
    </h3>

    <% end %>
  </tbody>
<%= link_to 'Create New List', new_list_path %>
